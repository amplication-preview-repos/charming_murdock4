/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { BranchService } from "../branch.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { BranchCreateInput } from "./BranchCreateInput";
import { Branch } from "./Branch";
import { BranchFindManyArgs } from "./BranchFindManyArgs";
import { BranchWhereUniqueInput } from "./BranchWhereUniqueInput";
import { BranchUpdateInput } from "./BranchUpdateInput";
import { PointsFindManyArgs } from "../../points/base/PointsFindManyArgs";
import { Points } from "../../points/base/Points";
import { PointsWhereUniqueInput } from "../../points/base/PointsWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class BranchControllerBase {
  constructor(
    protected readonly service: BranchService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Branch })
  @nestAccessControl.UseRoles({
    resource: "Branch",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createBranch(@common.Body() data: BranchCreateInput): Promise<Branch> {
    return await this.service.createBranch({
      data: {
        ...data,

        brand: data.brand
          ? {
              connect: data.brand,
            }
          : undefined,
      },
      select: {
        brand: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,
        name: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [Branch] })
  @ApiNestedQuery(BranchFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Branch",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async branches(@common.Req() request: Request): Promise<Branch[]> {
    const args = plainToClass(BranchFindManyArgs, request.query);
    return this.service.branches({
      ...args,
      select: {
        brand: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,
        name: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Branch })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Branch",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async branch(
    @common.Param() params: BranchWhereUniqueInput
  ): Promise<Branch | null> {
    const result = await this.service.branch({
      where: params,
      select: {
        brand: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,
        name: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Branch })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Branch",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateBranch(
    @common.Param() params: BranchWhereUniqueInput,
    @common.Body() data: BranchUpdateInput
  ): Promise<Branch | null> {
    try {
      return await this.service.updateBranch({
        where: params,
        data: {
          ...data,

          brand: data.brand
            ? {
                connect: data.brand,
              }
            : undefined,
        },
        select: {
          brand: {
            select: {
              id: true,
            },
          },

          createdAt: true,
          id: true,
          name: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Branch })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Branch",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteBranch(
    @common.Param() params: BranchWhereUniqueInput
  ): Promise<Branch | null> {
    try {
      return await this.service.deleteBranch({
        where: params,
        select: {
          brand: {
            select: {
              id: true,
            },
          },

          createdAt: true,
          id: true,
          name: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/pointsItems")
  @ApiNestedQuery(PointsFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Points",
    action: "read",
    possession: "any",
  })
  async findPointsItems(
    @common.Req() request: Request,
    @common.Param() params: BranchWhereUniqueInput
  ): Promise<Points[]> {
    const query = plainToClass(PointsFindManyArgs, request.query);
    const results = await this.service.findPointsItems(params.id, {
      ...query,
      select: {
        branch: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,
        points: true,
        updatedAt: true,

        user: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/pointsItems")
  @nestAccessControl.UseRoles({
    resource: "Branch",
    action: "update",
    possession: "any",
  })
  async connectPointsItems(
    @common.Param() params: BranchWhereUniqueInput,
    @common.Body() body: PointsWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      pointsItems: {
        connect: body,
      },
    };
    await this.service.updateBranch({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/pointsItems")
  @nestAccessControl.UseRoles({
    resource: "Branch",
    action: "update",
    possession: "any",
  })
  async updatePointsItems(
    @common.Param() params: BranchWhereUniqueInput,
    @common.Body() body: PointsWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      pointsItems: {
        set: body,
      },
    };
    await this.service.updateBranch({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/pointsItems")
  @nestAccessControl.UseRoles({
    resource: "Branch",
    action: "update",
    possession: "any",
  })
  async disconnectPointsItems(
    @common.Param() params: BranchWhereUniqueInput,
    @common.Body() body: PointsWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      pointsItems: {
        disconnect: body,
      },
    };
    await this.service.updateBranch({
      where: params,
      data,
      select: { id: true },
    });
  }
}
